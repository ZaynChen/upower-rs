[options]
library = "UPowerGlib"
version = "1.0"
min_cfg_version = "1.91.0"
target_path = "."
girs_directories = ["../../upower-gir-files"]
work_mode = "sys"
single_version_file = true

external_libraries = ["GLib", "Gio", "GObject"]
